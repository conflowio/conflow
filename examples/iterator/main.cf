colors := ["green", "red", "blue"]
shapes := ["circle", "rectangle", "triangle"]

iterator {
    count = len(root.colors)
    i1 it
}

print_colors do {
    color := root.colors[i1.value]

    iterator {
        count = len(root.shapes)
        i2 it
    }

    println {
        value = print_colors.color + " " + root.shapes[i2.value]
    }
}
