ticker second_ticker {
    interval = 1s
    count = 3

    tick tick {
        prefix := "Time is: "
    }
}

print {
    value = tick.prefix + string(tick.time)
    newline = true
}

print {
    value = "Number of ticks: " + string(second_ticker.ticks)
    newline = true
}